# Discord Bot Configuration
BOT_TOKEN=your_discord_bot_token_here
WEBHOOK_LOGS_URL=https://discord.com/api/webhooks/your_webhook_url
GUILD_ID=your_guild_id_optional

# Supabase Database Configuration
# Connection Pooler URL (used for serverless/edge environments)
DATABASE_URL=postgresql://postgres.your-project-ref:password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1

# Direct Connection URL (used for migrations and schema management)
DIRECT_URL=postgresql://postgres.your-project-ref:password@aws-0-region.pooler.supabase.com:5432/postgres

# Como obter as URLs do Supabase:
# 1. Acesse seu projeto no Supabase Dashboard (https://app.supabase.com)
# 2. Vá em Settings > Database
# 3. Na seção "Connection string", você encontrará:
#    - Connection Pooling (use esta para DATABASE_URL) - porta 6543
#    - Direct connection (use esta para DIRECT_URL) - porta 5432
# 4. Substitua [YOUR-PASSWORD] pela senha do seu banco de dados

# Optional Node.js options
NODE_OPTIONS=
